# Pinned to the versions used for the 2026-04-22 release build (Zenodo DOI
# 10.5281/zenodo.19363075 and subsequent bug-fix revisions). >= kept to allow
# minor patch-level upgrades without forcing environment churn; if you need a
# byte-identical rebuild, pin to exact == versions.

pandas>=2.3.2
pyarrow>=18.1.0
matplotlib>=3.10.5
numpy>=2.3.1

# Notebook runtime for notebooks/quickstart.ipynb.
jupyter>=1.0
nbformat>=5.9
